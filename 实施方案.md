# 一句话实施

使用 Python 预处理 ECDICT，筛选音标、中文释义、等级和词形字段，生成词形原形映射表，并按单词前两个字母拆分为静态 JSON；浏览器分析 TXT 后按需加载相应分片，并使用 IndexedDB 缓存。把用户的分析记录保存在浏览器本地,仅保存分析记录

```
Next.js
TypeScript
Tailwind CSS
shadcn/ui
Web Worker
IndexedDB
@react-pdf/renderer
Vercel
```

## ECDICT 预处理与分片

```text
下载 ECDICT 原始数据
↓
读取 CSV 或 SQLite 词典
↓
筛选网站需要的字段
↓
清洗单词、音标、释义和等级标签
↓
解析 exchange 词形变化字段
↓
生成“词形 → 单词原形”映射表
↓
按单词前两个字母拆分词典
↓
压缩并输出静态 JSON 文件
↓
浏览器按需加载对应分片
↓
使用 IndexedDB 缓存已下载分片
```

### 保留的词典字段

```text
word          单词原形
phonetic      音标
translation   中文释义
tag           CET4、CET6、TEM4、TEM8、TOEFL、IELTS、GRE 等标签
exchange      复数、过去式、过去分词、现在分词、比较级等词形变化
```

不需要的字段可以删除，减少词典文件体积，例如：

```text
英文释义
柯林斯星级
牛津核心词标记
外部音频地址
不需要的词频数据
无关扩展字段
```

### 生成词形映射表

根据 ECDICT 的 `exchange` 字段和 `lemma.en.txt`，生成：

```json
{
  "went": "go",
  "gone": "go",
  "going": "go",
  "gave": "give",
  "given": "give",
  "children": "child",
  "teeth": "tooth"
}
```

用户上传小说后，程序先将不同词形转换为原形，再统计词频：

```text
run + runs + running + ran
↓
统一统计为 run
```

同时保留不同词形各自出现的次数：

```json
{
  "lemma": "run",
  "count": 18,
  "forms": {
    "run": 10,
    "runs": 3,
    "running": 4,
    "ran": 1
  }
}
```

### 生成词典分片

按照单词前两个字母拆分：

```text
public/dict/
├── aa.json
├── ab.json
├── ac.json
├── ...
├── th.json
├── un.json
└── manifest.json
```

每个分片内部保存对应的词典数据：

```json
{
  "abandon": {
    "phonetic": "əˈbændən",
    "translation": "v. 放弃；遗弃",
    "tags": ["cet4", "cet6", "toefl"]
  },
  "ability": {
    "phonetic": "əˈbɪləti",
    "translation": "n. 能力；才能",
    "tags": ["cet4", "cet6"]
  }
}
```

### 浏览器查询方式

用户上传 TXT 后，程序先统计文中出现的不同单词。

假设文中出现：

```text
abandon
ability
character
difficult
understand
```

程序只请求：

```text
ab.json
ch.json
di.json
un.json
```

不需要一次下载整个 ECDICT。

查询流程：

```text
提取所有唯一单词
↓
计算每个单词对应的分片名称
↓
去重需要加载的分片
↓
并行下载 JSON 分片
↓
保存到 IndexedDB
↓
查询音标、释义和等级
↓
合并到词频统计结果
```

### 本地缓存

第一次分析小说时：

```text
从网站 CDN 下载词典分片
↓
保存到 IndexedDB
```

以后再次遇到同一个分片时：

```text
优先从 IndexedDB 读取
↓
无需重复下载
```

分片文件需要带版本号：

```json
{
  "version": "2026-08-01",
  "prefixLength": 2
}
```

当词典更新时，可以根据版本号清理旧缓存。

### 最终产物

ECDICT 预处理脚本最终生成：

```text
public/
├── dict/
│   ├── manifest.json
│   ├── aa.json
│   ├── ab.json
│   └── ...
├── lemma/
│   └── lemma-map.json
└── stopwords/
    ├── basic.json
    └── strong.json
```

对应作用：

```text
dict/*.json       查询音标、中文释义、考试等级
lemma-map.json    将不同词形合并为单词原形
basic.json        过滤 a、the、is 等基础词
strong.json       过滤更多常见简单词
```
